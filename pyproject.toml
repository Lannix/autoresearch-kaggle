[project]
name = "autoresearch-pinn"
version = "0.1.0"
description = "Autonomous PINN research swarm for LLE"
readme = "README.md"
requires-python = ">=3.10"
dependencies =[
    "kaggle>=1.6.0",
    "python-dotenv>=1.0.1"
]